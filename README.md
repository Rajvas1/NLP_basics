#steps to add to github
1. git init
2.# create a repo over in your github
3.git branch -M main
4.git remote add origin < enter the url you created>
5.git add . 
6.git commit -m "give some meaning full message"
7.git push -u origin main